FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install --legacy-peer-deps

COPY . .

# Build TypeScript
RUN npm run build

# Render fournit le port via $PORT
EXPOSE 3000

CMD ["npm", "start"]
